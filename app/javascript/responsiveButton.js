// Get all buttons 
// const buttons = document.querySelectorAll(".btn-review");

// Hide button after clicking it using

// console.log('buttonhidden')

const hiddenButton = () => {
    console.log('button clicked')
    buttons.forEach(b => { console.log('b')})   
}

export default hiddenButton();

