<div class="container">
  <div class="header">
    <h1>New booking</h1>
  </div>

  <%= turbo_frame_tag @booking do %>
    <%= render "form", schedule: @schedule, booking: @booking %>
  <% end %>
</div>
